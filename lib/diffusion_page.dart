import 'package:flutter/material.dart';

class DiffusionPage extends StatelessWidget {
  const DiffusionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Diffusion Page')),
      body: const Center(child: Text('Diffusion Page Content')),
    );
  }
}
